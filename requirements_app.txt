# TataStrive Analytics - Combined Dependencies
# Install with: pip install -r requirements_app.txt

# GUI Framework
PyQt6>=6.4.0
PyQt6-Qt6>=6.4.0

# Core dependencies (numpy<2 required: PyTorch 2.0.x fails with numpy 2.x)
numpy>=1.21,<2.0
typing_extensions  # Required by ultralytics, onnxruntime
setuptools>=65,<81  # boxmot imports pkg_resources (removed in setuptools 82+)
opencv-python
# PyTorch 2.0.1 CPU - stable, avoids DLL errors on Windows (use fix_pytorch.bat if issues)
torch==2.0.1
torchvision==0.15.2
scipy
tqdm

# AI/ML Libraries
ultralytics>=8.0.0
omegaconf  # Required for yolov8n-face.pt (deepghs/yolo-face)
boxmot==10.0.45
insightface
# Face analysis: onnxruntime works on CPU & GPU. For GPU speed: pip install onnxruntime-gpu (replace, don't add)
onnxruntime>=1.16.0
onnx>=1.14.0  # Required by insightface (numpy_helper)
lapx
easyocr

# API clients (for VLM metadata)
groq>=0.4.0

# Environment variables
python-dotenv

# BigQuery sync (Tata Strive Analytics cloud reporting)
google-cloud-bigquery>=3.10.0
google-auth>=2.20.0

# Optional: Intel CPU acceleration (2-3x faster on Intel CPUs)
# Uncomment to install: pip install openvino
openvino>=2024.0.0

# Packaging
pyinstaller>=6.0.0
pefile  # Required by PyInstaller on Windows
